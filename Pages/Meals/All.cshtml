@page
@model Ghostchef.Pages.Meals.AllModel

@{
    ViewData["Title"] = "Færdigretter";
}

<h1>@ViewData["Title"]</h1>

<div class="container">
    <div class="row">
        @foreach (var meal in Model.Data)
        {
            var cardClass = meal.InStock ? "card" : "card bg-light text-secondary";
            <div class="col-md-4 mb-3">
                <div class="@cardClass">
                    <div class="card-header">
                        @meal.Name
                    </div>
                    <div class="card-body">
                        <p class="card-text">@meal.Description</p>
                        <h5>Ingredients:</h5>
                        <ul>
                            @foreach (var ingredient in meal.Ingredients)
                            {
                                <li>@ingredient.ToString()</li>
                            }
                        </ul>
                        <h5>Diets:</h5>
                        <ul>
                            @foreach (var diet in meal.Diets)
                            {
                                <li>@diet.ToString()</li>
                            }
                        </ul>
                        <h5>Allergies:</h5>
                        <ul>
                            @foreach (var allergy in meal.Allergies)
                            {
                                <li>@allergy.ToString()</li>
                            }
                        </ul>
                        @if (!meal.InStock)
                        {
                            <p class="text-danger" style="font-size: 20px; font-weight: bold;">UDSOLGT</p>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>